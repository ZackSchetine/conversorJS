export default function numeroNegativo(valor) {
    if (valor < 0) {
        return console.log("\nCheque o valor inserido. somente números positivos são válidos");
    } else {
        return valor;
    }
}